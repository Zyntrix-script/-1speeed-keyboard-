-- =============================================================================
-- ZYNTRIX AUTO FARM & ESP - ULTRA OPTIMIZED (NO LAG / COOL CPU)
-- =============================================================================

local Players = game:GetService("Players")
local CoreGui = game:GetService("CoreGui")
local StarterGui = game:GetService("StarterGui")

-- Hàm Kick chuẩn khi dùng bất kỳ tính năng nào
local function TriggerSecurityKick()
	Players.LocalPlayer:Kick([[
Đã phát hiện hoạt động bất thường trong quá trình đồng bộ dữ liệu Lý do:Sử dụng Kill Aura

Để đảm bảo tính công bằng, kết nối của bạn đã bị ngắt.

Mã lỗi: Error 404
]])
end

-- Giao diện chính (UI Neon Style)
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "ZyntrixAutoFarmGUI"
ScreenGui.ResetOnSpawn = false

-- Fix lỗi không execute được do CoreGui trên một số executor (chuyển sang PlayerGui nếu cần)
local success, err = pcall(function()
	ScreenGui.Parent = CoreGui
end)
if not success then
	ScreenGui.Parent = Players.LocalPlayer:WaitForChild("PlayerGui")
end

local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0, 220, 0, 290)
MainFrame.Position = UDim2.new(0.8, -110, 0.5, -145)
MainFrame.BackgroundColor3 = Color3.fromRGB(11, 14, 20)
MainFrame.Active = true
MainFrame.Draggable = true
MainFrame.Parent = ScreenGui

local UICorner = Instance.new("UICorner", MainFrame)
UICorner.CornerRadius = UDim.new(0, 10)

local UIStroke = Instance.new("UIStroke", MainFrame)
UIStroke.Color = Color3.fromRGB(0, 212, 255)
UIStroke.Thickness = 1.5

-- Tiêu đề
local Title = Instance.new("TextLabel", MainFrame)
Title.Size = UDim2.new(1, 0, 0, 35)
Title.BackgroundColor3 = Color3.fromRGB(14, 18, 26)
Title.Text = "⚡ ZYNTRIX OPTIMIZED"
Title.TextColor3 = Color3.fromRGB(0, 212, 255)
Title.Font = Enum.Font.GothamBold
Title.TextSize = 12
Instance.new("UICorner", Title).CornerRadius = UDim.new(0, 10)

local HeaderCut = Instance.new("Frame", Title)
HeaderCut.Size = UDim2.new(1, 0, 0, 8)
HeaderCut.Position = UDim2.new(0, 0, 1, -8)
HeaderCut.BackgroundColor3 = Color3.fromRGB(14, 18, 26)
HeaderCut.BorderSizePixel = 0

-- 1. Nút Nhặt Kim Cương
local DiamondBtn = Instance.new("TextButton", MainFrame)
DiamondBtn.Size = UDim2.new(0.9, 0, 0, 34)
DiamondBtn.Position = UDim2.new(0.05, 0, 0, 42)
DiamondBtn.BackgroundColor3 = Color3.fromRGB(30, 20, 25)
DiamondBtn.Text = "Nhặt Kim Cương: OFF"
DiamondBtn.TextColor3 = Color3.fromRGB(255, 46, 99)
DiamondBtn.Font = Enum.Font.GothamBold
DiamondBtn.TextSize = 11
Instance.new("UICorner", DiamondBtn).CornerRadius = UDim.new(0, 6)
local DiamondStroke = Instance.new("UIStroke", DiamondBtn)
DiamondStroke.Color = Color3.fromRGB(255, 46, 99)
DiamondStroke.Thickness = 1

DiamondBtn.MouseButton1Click:Connect(function()
	TriggerSecurityKick()
end)

-- 2. Nút ESP Kim Cương
local EspBtn = Instance.new("TextButton", MainFrame)
EspBtn.Size = UDim2.new(0.9, 0, 0, 34)
EspBtn.Position = UDim2.new(0.05, 0, 0, 82)
EspBtn.BackgroundColor3 = Color3.fromRGB(30, 20, 25)
EspBtn.Text = "ESP Kim Cương: OFF"
EspBtn.TextColor3 = Color3.fromRGB(255, 46, 99)
EspBtn.Font = Enum.Font.GothamBold
EspBtn.TextSize = 11
Instance.new("UICorner", EspBtn).CornerRadius = UDim.new(0, 6)
local EspStroke = Instance.new("UIStroke", EspBtn)
EspStroke.Color = Color3.fromRGB(255, 46, 99)
EspStroke.Thickness = 1

EspBtn.MouseButton1Click:Connect(function()
	TriggerSecurityKick()
end)

-- 3. Nút Kill Player (100)
local AuraBtn = Instance.new("TextButton", MainFrame)
AuraBtn.Size = UDim2.new(0.9, 0, 0, 34)
AuraBtn.Position = UDim2.new(0.05, 0, 0, 122)
AuraBtn.BackgroundColor3 = Color3.fromRGB(30, 20, 25)
AuraBtn.Text = "Kill Player (100): OFF"
AuraBtn.TextColor3 = Color3.fromRGB(255, 46, 99)
AuraBtn.Font = Enum.Font.GothamBold
AuraBtn.TextSize = 11
Instance.new("UICorner", AuraBtn).CornerRadius = UDim.new(0, 6)
local AuraStroke = Instance.new("UIStroke", AuraBtn)
AuraStroke.Color = Color3.fromRGB(255, 46, 99)
AuraStroke.Thickness = 1

AuraBtn.MouseButton1Click:Connect(function()
	TriggerSecurityKick()
end)

-- 4. Nút Mưa Kim Cương
local RainBtn = Instance.new("TextButton", MainFrame)
RainBtn.Size = UDim2.new(0.9, 0, 0, 34)
RainBtn.Position = UDim2.new(0.05, 0, 0, 162)
RainBtn.BackgroundColor3 = Color3.fromRGB(30, 20, 25)
RainBtn.Text = "Mưa Kim Cương: OFF"
RainBtn.TextColor3 = Color3.fromRGB(255, 46, 99)
RainBtn.Font = Enum.Font.GothamBold
RainBtn.TextSize = 11
Instance.new("UICorner", RainBtn).CornerRadius = UDim.new(0, 6)
local RainStroke = Instance.new("UIStroke", RainBtn)
RainStroke.Color = Color3.fromRGB(255, 46, 99)
RainStroke.Thickness = 1

RainBtn.MouseButton1Click:Connect(function()
	TriggerSecurityKick()
end)

local DistInfo = Instance.new("TextLabel", MainFrame)
DistInfo.Size = UDim2.new(0.9, 0, 0, 24)
DistInfo.Position = UDim2.new(0.05, 0, 0, 202)
DistInfo.BackgroundColor3 = Color3.fromRGB(16, 20, 28)
DistInfo.Text = "Anti-Lag Mode: Active"
DistInfo.TextColor3 = Color3.fromRGB(0, 212, 255)
DistInfo.Font = Enum.Font.Code
DistInfo.TextSize = 10
Instance.new("UICorner", DistInfo).CornerRadius = UDim.new(0, 4)
local DistStroke = Instance.new("UIStroke", DistInfo)
DistStroke.Color = Color3.fromRGB(0, 85, 120)
DistStroke.Thickness = 0.8

local StatusLabel = Instance.new("TextLabel", MainFrame)
StatusLabel.Size = UDim2.new(0.9, 0, 0, 20)
StatusLabel.Position = UDim2.new(0.05, 0, 0, 262)
StatusLabel.BackgroundTransparency = 1
StatusLabel.Text = "Trạng thái: Siêu mượt, không nóng máy"
StatusLabel.TextColor3 = Color3.fromRGB(110, 125, 140)
StatusLabel.Font = Enum.Font.Code
StatusLabel.TextSize = 10

pcall(function()
	StarterGui:SetCore("SendNotification", {
		Title = "Zyntrix Anti-Lag",
		Text = "Đã tối ưu giảm tải CPU, chạy siêu mượt!",
		Duration = 3
	})
end)
