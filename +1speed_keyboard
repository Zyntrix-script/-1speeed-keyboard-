--[[
=======================================================================================
📜 ZYNTRIX MULTI-PANEL: WINS FARM EDITION
👤 Co-Authored: Soner1x & Zyntrix Tech
🔒 Status: Secured with Zyntrix Key System
=======================================================================================
--]]

-- CẤU HÌNH KEY HỆ THỐNG
local CORRECT_KEY = "zyntrix"

local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local CoreGui = game:GetService("CoreGui")
local UserInputService = game:GetService("UserInputService")
local VirtualUser = game:GetService("VirtualUser")

local player = Players.LocalPlayer
local farmEnabled = false
local antiAfkEnabled = false
local antiAfkConnection = nil
local menuVisible = true

-- Bảng màu Zyntrix Cyberpunk
local C = {
    Bg = Color3.fromRGB(11,14,20), 
    Cd = Color3.fromRGB(18,23,33), 
    Tb = Color3.fromRGB(14,18,26), 
    Cyan = Color3.fromRGB(0,212,255), 
    Dim = Color3.fromRGB(0,85,120), 
    On = Color3.fromRGB(0,255,136), 
    Off = Color3.fromRGB(255,46,99), 
    Wht = Color3.fromRGB(240,245,255)
}

-- Tọa độ Farm đường bay (Tween Waypoints)
local waypoints = {
    Vector3.new(-1457, -161, -913),
    Vector3.new(-1465, -160, -938),
    Vector3.new(-1483, -135, -764),
    Vector3.new(-1476, -87, -591),
    Vector3.new(-1465, -62, -414),
    Vector3.new(-1464, -57, -255),
    Vector3.new(-1461, -90, -122),
    Vector3.new(-1482, -56, -16)
}

-- Hàm tạo hiệu ứng Draggable kéo thả UI ổn định
local function makeDraggable(frame)
    local dragging, dragInput, dragStart, startPos
    frame.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            dragStart = input.Position
            startPos = frame.Position
            input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then dragging = false end
            end)
        end
    end)
    frame.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
            dragInput = input
        end
    end)
    UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and dragging then
            local delta = input.Position - dragStart
            frame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
        end
    end)
end

-- Dọn dẹp bản cũ nếu trùng tên
if CoreGui:FindFirstChild("ZyntrixWinsFarm") then
    CoreGui.ZyntrixWinsFarm:Destroy()
end

local SG = Instance.new("ScreenGui", CoreGui)
SG.Name = "ZyntrixWinsFarm"
SG.ResetOnSpawn = false

----------------------------------------------------
-- [PHẦN 1: GIAO DIỆN NHẬP KEY SYSTEM]
----------------------------------------------------
local KF = Instance.new("Frame", SG)
KF.Size = UDim2.new(0, 260, 0, 150)
KF.Position = UDim2.new(0.5, -130, 0.5, -75)
KF.BackgroundColor3 = C.Bg
KF.Active = true
makeDraggable(KF)
Instance.new("UICorner", KF).CornerRadius = UDim.new(0, 10)
local KFS = Instance.new("UIStroke", KF) KFS.Color = C.Cyan KFS.Thickness = 1

local KTB = Instance.new("Frame", KF)
KTB.Size = UDim2.new(1, 0, 0, 35)
KTB.BackgroundColor3 = C.Tb
Instance.new("UICorner", KTB).CornerRadius = UDim.new(0, 10)
local KFix = Instance.new("Frame", KTB) KFix.Size = UDim2.new(1, 0, 0, 10) KFix.Position = UDim2.new(0, 0, 1, -10) KFix.BackgroundColor3 = C.Tb KFix.BorderSizePixel = 0

local KT = Instance.new("TextLabel", KTB)
KT.Size = UDim2.new(1, 0, 1, 0)
KT.BackgroundTransparency = 1
KT.Text = "ZYNTRIX SECURITY SYSTEM"
KT.TextColor3 = C.Cyan
KT.Font = Enum.Font.GothamBold
KT.TextSize = 10

local KeyBox = Instance.new("TextBox", KF)
KeyBox.Size = UDim2.new(1, -30, 0, 35)
KeyBox.Position = UDim2.new(0, 15, 0, 55)
KeyBox.BackgroundColor3 = C.Cd
KeyBox.TextColor3 = C.Wht
KeyBox.PlaceholderText = "Nhập Key xác thực..."
KeyBox.Text = ""
KeyBox.ClearTextOnFocus = false
KeyBox.Font = Enum.Font.GothamSemibold
KeyBox.TextSize = 11
Instance.new("UICorner", KeyBox).CornerRadius = UDim.new(0, 6)
local KBS = Instance.new("UIStroke", KeyBox) KBS.Color = C.Dim KBS.Thickness = 0.8

local CheckBtn = Instance.new("TextButton", KF)
CheckBtn.Size = UDim2.new(1, -30, 0, 30)
CheckBtn.Position = UDim2.new(0, 15, 0, 105)
CheckBtn.BackgroundColor3 = Color3.fromRGB(15, 35, 50)
CheckBtn.Text = "XÁC NHẬN TRUY CẬP"
CheckBtn.TextColor3 = C.Cyan
CheckBtn.Font = Enum.Font.GothamBold
CheckBtn.TextSize = 11
Instance.new("UICorner", CheckBtn).CornerRadius = UDim.new(0, 6)
local CBS = Instance.new("UIStroke", CheckBtn) CBS.Color = C.Cyan CBS.Thickness = 0.8

----------------------------------------------------
-- [PHẦN 2: PANEL CHÍNH & PHẦN PHỤ (ẨN BAN ĐẦU)]
----------------------------------------------------

-- 1. Nút Watermark thu nhỏ / mở nhanh
local WatermarkButton = Instance.new("TextButton", SG)
WatermarkButton.Size = UDim2.new(0, 90, 0, 30)
WatermarkButton.Position = UDim2.new(0.05, 0, 0.28, 0)
WatermarkButton.BackgroundColor3 = C.Bg
WatermarkButton.Text = "Zyntrix x Soner"
WatermarkButton.TextColor3 = C.Cyan
WatermarkButton.Font = Enum.Font.GothamBold
WatermarkButton.TextSize = 10
WatermarkButton.Visible = false
makeDraggable(WatermarkButton)
Instance.new("UICorner", WatermarkButton).CornerRadius = UDim.new(0, 6)
local WMS = Instance.new("UIStroke", WatermarkButton) WMS.Color = C.Cyan WMS.Thickness = 1

-- 2. Khung điều khiển chính
local MainFrame = Instance.new("Frame", SG)
MainFrame.Size = UDim2.new(0, 220, 0, 175)
MainFrame.Position = UDim2.new(0.05, 0, 0.34, 0)
MainFrame.BackgroundColor3 = C.Bg
MainFrame.Active = true
MainFrame.Visible = false
makeDraggable(MainFrame)
Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 12)
local MS = Instance.new("UIStroke", MainFrame) MS.Color = C.Cyan MS.Thickness = 1

local TB = Instance.new("Frame", MainFrame) TB.Size = UDim2.new(1, 0, 0, 38) TB.BackgroundColor3 = C.Tb Instance.new("UICorner", TB).CornerRadius = UDim.new(0, 12)
local TFix = Instance.new("Frame", TB) TFix.Size = UDim2.new(1, 0, 0, 10) TFix.Position = UDim2.new(0, 0, 1, -10) TFix.BackgroundColor3 = C.Tb TFix.BorderSizePixel = 0
local T = Instance.new("TextLabel", TB) T.Size = UDim2.new(0, 150, 1, 0) T.Position = UDim2.new(0, 12, 0, 0) T.BackgroundTransparency = 1 T.Text = "WINS FARM PANEL" T.TextColor3 = C.Cyan T.Font = Enum.Font.GothamBold T.TextSize = 11

-- Nút Tắt Script Hoàn Toàn (X)
local CloseButton = Instance.new("TextButton", TB)
CloseButton.Size = UDim2.new(0, 24, 0, 24)
CloseButton.Position = UDim2.new(1, -32, 0.5, -12)
CloseButton.BackgroundColor3 = Color3.fromRGB(35, 20, 25)
CloseButton.Text = "×"
CloseButton.TextColor3 = C.Off
CloseButton.Font = Enum.Font.GothamBold
CloseButton.TextSize = 16
Instance.new("UICorner", CloseButton).CornerRadius = UDim.new(0, 5)
local CBS_Close = Instance.new("UIStroke", CloseButton) CBS_Close.Color = C.Off CBS_Close.Thickness = 0.8

CloseButton.MouseButton1Click:Connect(function()
    farmEnabled = false
    antiAfkEnabled = false
    if antiAfkConnection then antiAfkConnection:Disconnect() end
    SG:Destroy()
end)

-- Nút bật Auto Farm
local ToggleButton = Instance.new("TextButton", MainFrame)
ToggleButton.Size = UDim2.new(1, -24, 0, 38)
ToggleButton.Position = UDim2.new(0, 12, 0, 54)
ToggleButton.BackgroundColor3 = Color3.fromRGB(30, 20, 25)
ToggleButton.Text = "AUTO FARM: OFF"
ToggleButton.TextColor3 = C.Off
ToggleButton.Font = Enum.Font.GothamBold
ToggleButton.TextSize = 11
Instance.new("UICorner", ToggleButton).CornerRadius = UDim.new(0, 7)
local TBS_Toggle = Instance.new("UIStroke", ToggleButton) TBS_Toggle.Color = C.Off TBS_Toggle.Thickness = 0.8

-- Nút bật Anti AFK
local AntiAfkButton = Instance.new("TextButton", MainFrame)
AntiAfkButton.Size = UDim2.new(1, -24, 0, 38)
AntiAfkButton.Position = UDim2.new(0, 12, 0, 106)
AntiAfkButton.BackgroundColor3 = Color3.fromRGB(30, 20, 25)
AntiAfkButton.Text = "ANTI AFK: OFF"
AntiAfkButton.TextColor3 = C.Off
AntiAfkButton.Font = Enum.Font.GothamBold
AntiAfkButton.TextSize = 11
Instance.new("UICorner", AntiAfkButton).CornerRadius = UDim.new(0, 7)
local ABS_Anti = Instance.new("UIStroke", AntiAfkButton) ABS_Anti.Color = C.Off ABS_Anti.Thickness = 0.8

----------------------------------------------------
-- [PHẦN 3: XỬ LÝ SỰ KIỆN & VẬN HÀNH LOGIC]
----------------------------------------------------
WatermarkButton.MouseButton1Click:Connect(function()
    menuVisible = not menuVisible
    MainFrame.Visible = menuVisible
    TweenService:Create(WatermarkButton, TweenInfo.new(0.15), {BackgroundColor3 = menuVisible and C.Bg or Color3.fromRGB(15,35,50)}):Play()
end)

local function startTweenFarm()
    while farmEnabled do
        local character = player.Character
        local rootPart = character and character:FindFirstChild("HumanoidRootPart")
        if rootPart then
            for _, targetPos in ipairs(waypoints) do
                if not farmEnabled then break end
                local distance = (rootPart.Position - targetPos).Magnitude
                local speed = 220
                local duration = distance / speed
                local tweenInfo = TweenInfo.new(duration, Enum.EasingStyle.Linear)
                local tween = TweenService:Create(rootPart, tweenInfo, {CFrame = CFrame.new(targetPos)})
                tween:Play()
                tween.Completed:Wait()
            end
            if farmEnabled then
                task.wait(0.8)
                repeat task.wait(0.1) until not farmEnabled or (rootPart.Position.Z < -800)
            end
        else
            task.wait(0.5)
        end
        task.wait(0.1)
    end
end

ToggleButton.MouseButton1Click:Connect(function()
    farmEnabled = not farmEnabled
    local isOn = farmEnabled
    ToggleButton.Text = isOn and "AUTO FARM: ON" or "AUTO FARM: OFF"
    ToggleButton.TextColor3 = isOn and C.On or C.Off
    TBS_Toggle.Color = isOn and C.On or C.Off
    TweenService:Create(ToggleButton, TweenInfo.new(0.2), {BackgroundColor3 = isOn and Color3.fromRGB(15,40,32) or Color3.fromRGB(30,20,25)}):Play()
    if isOn then task.spawn(startTweenFarm) end
end)

AntiAfkButton.MouseButton1Click:Connect(function()
    antiAfkEnabled = not antiAfkEnabled
    local isOn = antiAfkEnabled
    AntiAfkButton.Text = isOn and "ANTI AFK: ON" or "ANTI AFK: OFF"
    AntiAfkButton.TextColor3 = isOn and C.On or C.Off
    ABS_Anti.Color = isOn and C.On or C.Off
    TweenService:Create(AntiAfkButton, TweenInfo.new(0.2), {BackgroundColor3 = isOn and Color3.fromRGB(15,40,32) or Color3.fromRGB(30,20,25)}):Play()
    
    if isOn then
        antiAfkConnection = player.Idled:Connect(function()
            VirtualUser:Button2Down(Vector2.new(0,0), workspace.CurrentCamera.CFrame)
            task.wait(0.5)
            VirtualUser:Button2Up(Vector2.new(0,0), workspace.CurrentCamera.CFrame)
        end)
    else
        if antiAfkConnection then
            antiAfkConnection:Disconnect()
            antiAfkConnection = nil
        end
    end
end)

-- Kiểm Tra Key Bảo Mật Hệ Thống Zyntrix
CheckBtn.MouseButton1Click:Connect(function()
    if KeyBox.Text == CORRECT_KEY then
        CheckBtn.Text = "KEY CHÍNH XÁC! ĐANG MỞ..."
        CheckBtn.TextColor3 = C.On
        CBS.Color = C.On
        task.wait(1)
        
        KF:Destroy() -- Xóa giao diện Key System
        MainFrame.Visible = true -- Kích hoạt hiển thị Panel chính
        WatermarkButton.Visible = true -- Kích hoạt nút đóng/mở nhanh
    else
        CheckBtn.Text = "SAI KEY! HỆ THỐNG KICK..."
        CheckBtn.TextColor3 = C.Off
        CBS.Color = C.Off
        task.wait(0.6)
        player:Kick("⚠️ [Zyntrix Security]: Bạn đã nhập sai mã Key hệ thống! Vui lòng lấy key đúng để truy cập script.")
    end
end)
